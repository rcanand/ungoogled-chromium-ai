# Add chrome://flags entry for AI Sidebar

--- a/chrome/browser/ungoogled_flag_entries.h
+++ b/chrome/browser/ungoogled_flag_entries.h
@@ -72,4 +72,8 @@
      "Remove Tabsearch Button",
      "Removes the tabsearch button from the tabstrip. ungoogled-chromium flag",
      kOsDesktop, SINGLE_VALUE_TYPE("remove-tabsearch-button")},
+    {"disable-ai-sidebar",
+     "Disable AI Sidebar",
+     "Disables the AI Chat sidebar feature. ungoogled-chromium flag",
+     kOsDesktop, SINGLE_VALUE_TYPE("disable-ai-sidebar")},
 #endif  // CHROME_BROWSER_UNGOOGLED_FLAG_ENTRIES_H_
